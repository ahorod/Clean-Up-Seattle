import { MeetupByMessPipe } from './meetup-by-mess.pipe';

describe('MeetupByMessPipe', () => {
  it('create an instance', () => {
    const pipe = new MeetupByMessPipe();
    expect(pipe).toBeTruthy();
  });
});
